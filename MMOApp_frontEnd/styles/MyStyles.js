import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    flex: 1,
  }, subject: {
    fontSize: 10,
    fontWeight: "bold",
    color: "#333",
});

export default styles;  